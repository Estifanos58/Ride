import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const Onboarding = () => {
  return (
    <SafeAreaView>
      <Text className='text-3xl text-green-500'>Onboarding</Text>
    </SafeAreaView>
  )
}

export default Onboarding